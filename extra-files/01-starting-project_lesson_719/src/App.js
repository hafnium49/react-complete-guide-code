import React from 'react';
import AddUser from './components/Users/AddUser';

/*
  TUTOR'S GUIDANCE:
  Here we render `<AddUser />` so its relatively "ugly" default HTML is painted 
  onto the screen successfully until we build the Card wrapper in the next module!
*/
function App() {
  return (
    <div>
      <AddUser />
    </div>
  );
}

export default App;
